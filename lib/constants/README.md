# `lib/constants/`

[← Back to `lib/`](../README.md) · [↑ Root README](../../README.md)

## Purpose

Centralized constants used across the app.

- `headerNames`: canonical HTTP header names (avoid naming conflicts with Next.js APIs)
- `storageKeys`: keys used for browser storage

