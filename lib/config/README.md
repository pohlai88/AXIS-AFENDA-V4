# `lib/config/`

[← Back to `lib/`](../README.md) · [↑ Root README](../../README.md)

## Purpose

Application configuration (brand/site config, feature flags, etc.).

- Prefer reading public env via `getPublicEnv()` (not direct `process.env`).

