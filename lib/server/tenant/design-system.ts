import "@/lib/server/only"

import { generateTenantCss } from "@/lib/shared/design-system/css"

/**
 * Tenant Design System CSS Generator
 *
 * Converts tenant design system settings into CSS variables that override
 * the base theme. Based on shadcn/ui's create implementation (MIT licensed).
 */

export { generateTenantCss }
