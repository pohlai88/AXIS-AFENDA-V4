# `lib/api/`

[← Back to `lib/`](../README.md) · [↑ Root README](../../README.md)

## Purpose

Client-side API helpers (fetch wrappers, endpoint builders).

- `client.ts`: `apiFetch` and typed error handling (`ApiFetchError`)
- `endpoints.ts`: central API route path builders

